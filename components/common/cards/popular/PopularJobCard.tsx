import React from "react";
import { Text, View } from "react-native";

import { PopularJobcardProps } from "@/type";

const PopularJobCard = ({ item }: PopularJobcardProps) => {
  return (
    <View>
      <Text>PopularJobCards</Text>
    </View>
  );
};

export default PopularJobCard;
